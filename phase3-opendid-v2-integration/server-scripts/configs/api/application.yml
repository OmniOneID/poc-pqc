spring:
  profiles:
    active: dev, lss

springdoc:
  server:
    url: http://10.48.17.123:6363

observer:
  enabled: false # did-observer-server 호출 ON/OFF (성능 측정 시 false 권장)
  url: http://127.0.0.1:18080
  service-name: did-api-server

logging:
  level:
    root: ERROR # 성능 측정 시 로그 최소화
    # application-logging.yml(내부)에서 debug로 설정된 패키지를 명시적으로 덮어씀
    org.omnione: ERROR
